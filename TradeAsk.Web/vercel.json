{
  "buildCommand": "npm install && npx ng build --configuration production",
  "outputDirectory": "dist/TradeAsk.Web/browser",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
